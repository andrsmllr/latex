%
% text_formatting
% see: http://en.wikibooks.org/wiki/LaTeX/Text_Formatting
%

\documentclass[11pt,a4paper,titlepage,onecolumn]{article}

%
% Change the value of the inter line spacing (must be done within preamble).
%
\linespread{1.3} % one and a half timesthe normal line spacing.
%\linespread{1.6} % two times the normal line spacing

%
% The setspace package provides commands to customize the line spacing.
% Provides the environments \doublespace, \onehalfspace and \setstretch.
%
\usepackage{setspace}
%\singlespacing
\onehalfspacing
%\doublespacing
%\setstretch{1.3}

% Hyphenation rules can be added manually (will apply to currently active language).
\hyphenation{ca-ta-stro-phic Verilog}
%\hyphenpenalty=10000 % Avoid hyphenation all together by setting a huge penalty.

%
% Another package that helps with hyphenating words.
%
\usepackage{hyphenat}

%
% And another package that helps with hyphenating words.
%
%\usepackage[shortcuts]{extdash} % Not available in all Linux package repos.

%
% Package to widen text widths and reduce margin overruns.
%
\usepackage{geometry}

%
% Package to handle quotes for different languages and allows easy switching.
%
\usepackage{csquotes}

%
% Package which allows to disable ligatures.
% If litagures are enabled special sequences like -- for dash are supported!
%
\usepackage{microtype}
%\DisableLigatures{encoding=*, family=*}

%
% Package for subscript text.
%
\usepackage{changes}

\begin{document}

This paragraph has \\ default \\ line spacing.
 
\begin{doublespace}
  This paragraph has \\ double \\ line spacing.
\end{doublespace}
 
\begin{spacing}{2.5}
  This paragraph has \\ huge gaps \\ between lines.
\end{spacing}

% Non breaking space.
50~\$

\frenchspacing \par
Turn off additional spacing after a period. Can you see it?

\nonfrenchspacing \par
Turn off additional spacing after a period. Can you see it?

% Inserting horizontally stretched space.
This part is on the left \hfill and this part is on the right.

% Inserting vertically stretched space.
\vfill
This part is on the bottom.
\newpage

% Keep some words at the same line at all cost.
\mbox{All these words will be on the same line.}\\
\fbox{Same as above but with a box drawn around these words.}

% Creating a slash mark.
Words with slashes like input\slash output can be broken only if a special command for the slash is used.

% Text with superscript text.
This text contains \textsuperscript{superscript text}.

% Text with subscript text.
This text contains \textsubscript{subscript text}.

% Some "old" style numerals aka text figures.
% Note that not all numbers have equal height.
Old style numbers: \oldstylenums{0123456789}
New style numbers: 0123456789

% Dashes and hyphens, LaTeX knows 4 kinds of these.
hyphen ( \verb|-| ): - en dash ( \verb|--| ): -- em dash ( \verb|---| ): --- minus sign ( math mode ) $-$\\
Alternative commands: hyphen \hyp{} en dash \textendash{} em dash \textemdash{}

% Ellipsis
And he said some ellipsis related stuff \ldots the end.
\newline

% Special strings.
today: \today \\
TeX: \TeX \\
LaTeX: \LaTeX \\
LaTeX2e: \LaTeXe \\

\end{document}

